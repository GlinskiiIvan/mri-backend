export enum Gender {
  Male = 'male',
  Female = 'female',
}

export enum Status {
  Pending = 'pending',
  Processing = 'processing',
  Completed = 'completed',
  Failed = 'failed',
}

export enum Modality {
  MR = 'MR',
  CT = 'CT',
  XR = 'XR',
  US = 'US',
}